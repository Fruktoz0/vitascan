# Same-origin /api → Cloudflare /api/* rule (or Vite proxy as fallback)
VITE_API_URL=/api
# If /api somehow hits the Vite host, proxy to the real API machine
VITE_PROXY_TARGET=http://192.168.1.115:3005
