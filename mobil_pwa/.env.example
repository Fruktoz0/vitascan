# Same-origin /api → Cloudflare /api/* rule (or Vite proxy as fallback)
VITE_API_URL=/api
# Optional: bake VAPID public key at build time. The app prefers GET /api/notifications/vapid-public.
# VITE_VAPID_PUBLIC_KEY=
